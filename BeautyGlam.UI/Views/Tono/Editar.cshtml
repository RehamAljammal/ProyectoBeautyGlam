@model BeautyGlam.Abstracciones.ModelosParaUI.TonoDTO

@{
    ViewBag.Title = "Editar tono";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="section-space prod-page">

    <div class="container prod-container">

        <div class="prod-header">
            <h2 class="prod-title">Editar tono</h2>
            <p class="prod-subtitle">Modifica el tono seleccionado</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-6">

                <div class="prod-card p-4">

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        @Html.HiddenFor(m => m.id_Tono)

                        <div class="mb-3">
                            @Html.LabelFor(m => m.nombre, new { @class = "prod-label" })

                            @Html.TextBoxFor(m => m.nombre,
                                new { @class = "form-control prod-input" })

                            @Html.ValidationMessageFor(m => m.nombre)
                        </div>

                        <div class="mb-3">

                            <label class="prod-label">Estado</label>

                            <div>
                                @Html.CheckBoxFor(m => m.estado)
                                <span style="color:#6b4c7a;font-weight:650;">
                                    Activo
                                </span>
                            </div>

                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-4">

                            <a href="@Url.Action("Index","Tono")"
                               class="prod-btn prod-btn-soft">
                                Cancelar
                            </a>

                            <button type="submit"
                                    class="prod-btn">
                                Actualizar
                            </button>

                        </div>

                    }

                </div>

            </div>
        </div>

    </div>

</section>