@model BeautyGlam.Abstracciones.ModelosParaUI.MarcaDto

@{
    ViewBag.Title = "Eliminar Marca";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="section-space sup-page">
    <div class="container sup-container">

        <div class="sup-header">
            <h2 class="sup-title">Eliminar Marca</h2>
            <p class="sup-subtitle">Confirma si deseas inactivar esta marca 🗑️</p>
        </div>

        <div class="sup-card">
            <div class="sup-card-head">
                <span class="sup-badge">Confirmación</span>
            </div>

            <div class="sup-form">

                <p>
                    <strong>Nombre:</strong>
                    @Model.nombre
                </p>

                <p>
                    <strong>Estado actual:</strong>
                    @if (Model.estado)
                    {
                        <span class="sup-chip sup-chip-ok">Activo</span>
                    }
                    else
                    {
                        <span class="sup-chip sup-chip-off">Inactivo</span>
                    }
                </p>

                <hr />

                <p style="color:#7a1f2d; font-weight:700;">
                    ⚠️ Esta acción cambiará el estado de la marca.
                </p>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.id_Marca)

                    <div class="sup-actions mt-4">
                        <button type="submit" class="sup-btn sup-btn-danger">
                            Confirmar 🗑️
                        </button>

                        <a href="@Url.Action("ListaDeMarcas")"
                           class="sup-btn sup-btn-soft">
                            Cancelar
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</section>
