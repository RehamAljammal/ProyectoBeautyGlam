@{
    ViewBag.Title = "Repetir Diagnóstico";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="section-space text-center">
    <div class="container">
        <h3>¿Quieres ajustar tu rutina?</h3>
        <p class="text-muted mb-4">
            Puedes repetir el diagnóstico en cualquier momento.
        </p>

        <a href="@Url.Action("Cuestionario","AsistenteSkincare")"
           class="btn btn-dark btn-sm">
            Volver a empezar
        </a>
    </div>
</section>